<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/colorBackDark"
    tools:context="com.kroko.TicketToRideAssistant.Fragments.MainActivity">

    <Button
        android:id="@+id/draw_ticket"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/draw_ticket"
        android:textColor="#80000000"
        android:background="#4CAF50"
        android:onClick="onClickDrawTicket"/>

    <TextView
        android:id="@+id/tickets_header"
        android:layout_width="match_parent"
        android:layout_height="64sp"
        android:gravity="center|bottom"
        android:text="@string/tickets_header"/>

    <Switch
        android:id="@+id/switch_delete"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/reset_color"
        android:text="@string/locked" />

    <TextView
        android:id="@+id/delete_ticket_comment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/delete_station_comment"
        android:textColor="@color/reset_color"
        android:textSize="16sp"
        android:visibility="invisible" />

    <ListView
        android:id="@+id/list_tickets"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

</LinearLayout>